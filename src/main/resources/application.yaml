spring:
  application:
    name: Bank-app-user-service

security:
  jwt:
      secret: ${JWT_SECRET:senha-local}
      access-expiration: 900000    # 15m
      refresh-expiration: 604800000 # 7dias
  lockout:
    max-attempts: 5
    lock-duration: 900000 #15m
